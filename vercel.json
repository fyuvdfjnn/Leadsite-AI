{
  "version": 2,
  "buildCommand": "cd my-app && npm install && npm run build",
  "outputDirectory": "my-app/.next",
  "devCommand": "cd my-app && next dev",
  "installCommand": "cd my-app && npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
